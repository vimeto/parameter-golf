# Python deps. Install with: pip install -r requirements.txt
torch==2.9.1+cu129
sentencepiece
brotli
huggingface_hub
numpy
python-minifier

# FlashAttention 3 must be installed separately (not on PyPI):
# pip install --no-deps flash_attn_3 --find-links https://windreamer.github.io/flash-attention3-wheels/cu129_torch291/

# System dep (apt): lrzip (used by per-group lrzip compressor)
# apt-get install -y lrzip
